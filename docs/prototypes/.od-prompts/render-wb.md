为一个「数学建模 AI 辅导工具」生成「读题工作台」沉浸态页面，输出单个自包含 HTML，浅色+深色双主题（html[data-theme] 切换，默认浅色）。

【⚠️ 布局硬性要求（最重要，必须严格遵守）】
- 本页是「沉浸态工作区」：**禁止顶部横向导航栏**（不要放 读题/编程/设置 的 tab 顶栏）、**禁止步骤条**（不要 ①②③ 流程条）。
- 布局必须是**左右分栏**，自左到右共 4 列（用 flex/grid 实现）：
  1. 最左侧：**收起状态的图标窄栏（~52px）**，含顶部一个「返回小箭头」图标（点击展开全局左栏的入口）
  2. 主工作区（约 45% 宽）
  3. 书签竖栏（~40px）：竖排文字书签「选段精读(激活) / 知识卡片 / ＋更多」
  4. AI 面板（约 30% 宽）
- 桌面端优先：以 1440px 设计，整页铺满视口宽度，无窄栏居中。

【主工作区内容（自上而下）】
1. 顶部一行：返回小箭头 + 题目标题 + 右侧「保存」按钮；**题干上方居中位置一个醒目小按钮「✦ AI 整体解读」**（渐变蓝）
2. 题干正文（可滚动）：示例含三种角色语义色高亮（约束蓝/已知琥珀/目标绿，带胶囊标签）；一处选中态显示浮动工具栏（AI解读/提问/复制）
3. 附件表格预览：「表1 需求量」「表2 成本」两个示例表格卡片

【AI 面板（第 4 列，约 30%）】
- 由左侧书签竖栏切换内容：
  - 「选段精读」面板：角色判定卡（角色胶囊标签 + 置信度条 + 影响说明 + 引用原文 + 质疑/深挖按钮）
  - 「知识卡片」面板：卡片列表（决策树/线性回归/K-means/线性规划，可展开）
- 本页**不出现对话功能**、不出现聊天输入框

【空状态】另出一版主区（无题目时）：显示两个引导大入口「上传题干」「上传附件」（虚线框大按钮，横向并排）。

【风格】渐变蓝主色（浅 #3B82F6→#2563EB，深 #60A5FA→#3B82F6）、不对称构图、圆角 8-14px、字体圆润带锋利、留白中等、低装饰、无插件堆叠；图标一律线性 SVG 禁 emoji；data-od-id；交互态完整。

【重要提示】外部技能模板文件在本环境不可用，禁止读取任何外部文件；严格按本 prompt 的布局与内容生成，不要沿用任何旧版本的结构（无顶部导航、无步骤条）。
