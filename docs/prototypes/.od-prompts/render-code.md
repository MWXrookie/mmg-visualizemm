为一个「数学建模 AI 辅导工具」生成「编程工作台」页面（AI 编程 + 可视化），输出单个自包含 HTML，浅色+深色双主题（html[data-theme] 切换，默认浅色）。

【⚠️ 布局硬性要求】
- **禁止顶部横向导航栏、禁止步骤条**。左侧最边缘为收起状态图标窄栏（~52px，含返回小箭头）。
- 布局为**左右对半（各 50%）**，无右侧 AI 栏、无书签竖栏：
  1. 左 50%：代码区
  2. 右 50%：产出区（可视化）
- 桌面端优先：1440px 设计，整页铺满视口。

【左代码区（50%）】
1. 顶栏：「✨AI生成」「✨AI改进」「运行」「示例」按钮（✨AI生成 为主按钮，渐变蓝）
2. 代码编辑器（示例 Python 代码，语法高亮、行号，等宽字体）
3. 底部：输出日志区（示例运行结果/报错占位）

【✨AI生成 → 半屏覆盖弹窗（示例状态）】
- 标题「生成代码 — 基于什么生成？」
- ① 选择拆解块（来自梳理台）：☑ 目标函数怎么定？/ ☐ 约束条件怎么建模？/ ☐ 数据怎么处理？（多选）
- ② 自然语言补充描述：输入框（占位"用线性回归，数据在附件表1…"）
- 底部 [生成] [取消]

【右产出区（50%）】
1. 图表可视化区：示例 matplotlib 图表（如拟合曲线图）+ 说明「运行后产出图表，实时更新」
2. 调参面板：2-3 个参数滑块（如多项式次数/噪声），标注「改动即重算，图表/数值实时反馈」；一个结果数值显示

【风格】渐变蓝主色（浅 #3B82F6→#2563EB，深 #60A5FA→#3B82F6）、不对称构图、圆角 8-14px、字体圆润带锋利、留白中等、低装饰；代码区等宽字体；图标一律线性 SVG 禁 emoji；data-od-id；交互态完整。

【重要提示】外部技能模板文件不可用，禁止读取外部文件；严格按本 prompt 生成，不要沿用旧版本结构。
