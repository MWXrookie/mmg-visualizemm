为一个「数学建模 AI 辅导工具」生成「建模思路梳理台」沉浸态页面（产品特色：问题拆解），输出单个自包含 HTML，浅色+深色双主题（html[data-theme] 切换，默认浅色）。

【⚠️ 布局硬性要求】
- 沉浸态工作区：**禁止顶部横向导航栏、禁止步骤条**。
- 布局为左右分栏 4 列（flex/grid）：
  1. 最左侧：收起状态图标窄栏（~52px），顶部「返回小箭头」
  2. 主区（约 45%）：问题拆解区
  3. 书签竖栏（~40px）：竖排书签「对话(激活) / 知识卡片 / ＋更多」
  4. AI 面板（约 30%）
- 桌面端优先：1440px 设计，整页铺满视口，无窄栏居中。

【主区内容（自上而下）】
1. 顶部行：「问题选择器」（问题1 ▾ / 问题2，标签式切换）+ 右侧「导出思路」「生成代码」按钮
2. 拆解区：上下排列的**拆解块**（示例 2-3 个）：
   - 每块 = 标题行（如「目标函数怎么定？」）+ 折叠/展开 + 建模思考内容区 + 操作（编辑/关联/收起）
   - 其中一块呈**折叠态**（只显示标题行）
   - 底部「＋ 新建拆解块」按钮
3. AI 修改拆解块时：显示「修改预览」卡片（[确认][取消]），用户确认后才写入

【AI 面板（约 30%）】
- 由书签竖栏切换：
  - 「对话」面板：AI 对话流（示例 2-3 条，含一条用户指挥"把拆解块2补充数据思路"、AI 回复"已生成修改预览"）+ 底部输入框/发送按钮
  - 「知识卡片」面板：搜索框 + 卡片列表（决策树/线性回归/K-means/线性规划，可展开）
- 对话中 AI 可内嵌知识卡片（就地展示）

【风格】渐变蓝主色（浅 #3B82F6→#2563EB，深 #60A5FA→#3B82F6）、不对称构图、圆角 8-14px、字体圆润带锋利、留白中等、低装饰；图标一律线性 SVG 禁 emoji；data-od-id；交互态完整（含拆解块折叠过渡）。

【重要提示】外部技能模板文件不可用，禁止读取外部文件；严格按本 prompt 生成，不要沿用旧版本结构。
