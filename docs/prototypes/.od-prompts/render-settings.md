为一个「数学建模 AI 辅导工具」生成「模型设置」页，输出单个自包含 HTML，浅色+深色双主题（html[data-theme] 切换，默认浅色）。

【⚠️ 布局硬性要求】
- **桌面端优先**：以 1440px 设计，整页铺满视口宽度，禁止整页窄栏居中（≤900px）。
- 布局为**左右分栏**：左侧功能栏 + 右侧内容区：
  1. 左功能栏（约 220-260px）：顶部 Logo/产品名；中部功能导航「读题工作台 / 建模思路梳理台 / 编程工作台 / ＋更多(虚线扩展位)」；底部「模型设置(激活态高亮) / 主题切换」
  2. 右内容区：标题「模型设置」+ 说明文字；设置表单卡片在右内容区内居中（宽度 720-800px 即可，表单本身居中属正常）
- **禁止**：顶部横向导航、步骤条、任何对话/聊天 UI、引导模式开关。

【右内容区表单（自上而下）】
1. 标题 H1「模型设置」+ 说明：「Key 只保存在本浏览器（加密存储），不会上传服务器」
2. 「模型服务商」：4 个单选卡片或下拉（通义百炼 / DeepSeek / OpenAI / 自定义(OpenAI 兼容)），选中态渐变蓝边框
3. 「API Key」输入框（右侧「显示/隐藏」切换按钮，密码态）
4. 「Base URL」输入框（占位 https://api.deepseek.com/v1，自定义时显示）
5. 「模型名」输入框（占位 qwen-plus / deepseek-chat）
6. 操作行：「测试连接」主按钮（渐变蓝）→ 结果提示条（成功=绿 ✓「连接成功（模型 xxx）」/ 失败=红 ✗+人话原因）；「保存设置」ghost 按钮

【风格】渐变蓝主色（浅 #3B82F6→#2563EB，深 #60A5FA→#3B82F6）、不对称构图、圆角 8-14px、字体圆润带锋利、留白中等、低装饰；图标一律线性 SVG 禁 emoji（显示/隐藏眼睛、锁用 SVG）；data-od-id；交互态完整（输入 focus、按钮 hover/disabled）。

【重要提示】外部技能模板文件在本环境不可用，禁止读取任何外部文件；严格按本 prompt 的布局与内容生成，不要沿用任何旧版本结构。
