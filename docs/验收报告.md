# MMG_VisualizeMM 验收报告

> 日期：2026-08-29 ｜ 依据：[开发规划与验收.md](开发规划与验收.md) §4/§5/§6/§7
> 方法：代码走查 + 后端接口实测（本机 3088 端口）+ 前端构建验证；浏览器交互项标注「🖥 待人工」
> 状态图例：✅ 已通过（代码/接口证据）｜🖥 代码已就位，待浏览器人工验证｜⚠️ 部分/有说明｜❌ 未实现

---

## 1. 功能验收矩阵（对应 PRD F 编号）

| PRD 功能 | 验收口径 | 状态 | 证据 / 说明 |
|---------|---------|------|------------|
| F-101/102/103/104 上传解析 | 一道题+1 Excel 5 分钟完成；列名保留 | ✅ | 接口实测：xlsx 3 sheet、中文列名/富文本表头、CSV BOM 剥离、PDF 文本提取、TXT/MD；`.xls` 给出「另存为 .xlsx」提示；单文件 20MB 上限 + 413 JSON 提示 |
| F-201/202 整体解读 | 5 要素输出；列名↔术语关联 | 🖥 | prompt 含 5 要素 + 附件摘要联动，代码就位；需浏览器+真实 Key 目验 |
| F-203 流式输出 | SSE 流式，首字 <3s | ✅ | `/api/chat/stream` 已实现；用本地 mock provider 端到端验证 SSE 逐 chunk 转发 + 错误回退；前端三处（解读/划词/对话）流式打字机 + 非流式自动降级 |
| F-301 任意划词 | 选区弹菜单 | 🖥 | 交互已实现；**新增 ≤500 字限制**；键盘可达（tabIndex + onKeyUp） |
| F-302 角色判定 | 评测集 ≥90% | ✅ | 历史评测 10/10=100%；新增 `scripts/eval-role.mjs` 可一键复跑（env 配 Key） |
| F-303 双向引用 | 引用↔原文跳转 | ✅ | quote 全文高亮（首个带 id）+ 点击「引用原文」滚动定位 + 黄色闪烁动画 |
| F-304 连续深挖 | 选区锚点追问 | ✅ | 「继续深挖这段」带选区入对话 |
| F-305 置信度质疑 | 置信度 + 质疑重论证 | ✅ | 置信度展示 + 「🤔 质疑」重新论证 |
| F-401/402/403/404 对话深挖 | 问题链；防代做 | 🖥 | 引导模式系统提示（反问不代做，可开关）；步骤指示器 ①②③；需浏览器目验 |
| F-501~505 BYOK | Key 加密本地存；三分类错误；新手引导 | ✅ | **Key 已改为 WebCrypto AES-GCM 加密存储**（非安全上下文自动降级明文并保留功能）；错误分类 INVALID_KEY/QUOTA/NOT_FOUND/NETWORK 实测通过；设置页含 3 分钟指引 |
| F-504 模型切换 | 对话中切换模型 | ⚠️ | 仅设置页全局切换；「对话中切换」未实现（P0 项内弱需求） |
| F-601~604 知识卡片 | 概念触发；收藏持久化 | ✅ | **产品决策（2026-08-29）：卡片降级为读题附属**——无独立页面/导航入口，AI 输出（解读/对话/方案）提到建模概念时内嵌卡片就地出现（折叠→展开演示+收藏）；4 张卡：决策树/回归/K-means/**线性规划**；收藏 localStorage 持久化 |
| F-701~704 编程可视化 | 代码运行出图；错误人性化 | 🖥 | Pyodide 自动 loadPackage + matplotlib base64 回传 + 友好报错；Docker 沙箱为可选未做；需浏览器验证 |
| F-801~803 会话沉淀 | 保存/列表/导出 | ✅ | 自动保存/恢复（含附件）；**新增历史会话列表（继续/删除）+ 导出 Markdown**；⚠️ 卡片导出未做（收藏仅本地） |

## 2. UX 验收（§5）

| 条目 | 验收标准 | 状态 | 说明 |
|------|---------|------|------|
| 工作台布局 | 双栏 60/40；≤1280px 堆叠 | ✅ | `.split` 断点已从 1024 改为 1280 对齐文档 |
| 划词工具栏 | 选区上方浮出、不遮挡、键盘可达、<500 字 | ✅/🖥 | 绝对定位跟随选区；500 字限制已加；键盘可达代码就位 |
| 角色判定视觉 | 颜色+标签双通道；高亮可跳转；置信度可见 | ✅ | 语义色 + 文字标签；引用点击定位；置信度百分比 |
| AI 输出 | 全部流式；👍/👎+重新生成；引导样式区分 | ✅ | 本版实现 SSE 流式（解读/划词/对话）；反馈与重生成保留；guide 气泡样式 |
| 步骤指示器 | ①读题②精读③方向 | ✅ | 顶部步骤条，随状态点亮 |
| 错误信息 | 人话 + 行动建议 | ✅ | 三分类 Key 错误、413、超限、解析失败均有中文提示 |
| 无障碍 | 对比度≥4.5:1；焦点环；reduced-motion | ✅ | 正文 #1e293b/#fff（对比度 ~14:1）；**新增 :focus-visible 焦点环 + prefers-reduced-motion 关闭动画** |
| 移动端 | 375px 基本可用 | 🖥 | 响应式堆叠就位，需 375px 视口人工抽查 |
| 原型一致性 | 与 docs/prototypes 偏差 ≤5% | ⚠️ | 遵循「先功能后 UI」决策，UI 精调后置；原型见 `docs/prototypes/` |

## 3. 手动用例清单（§6.3，待浏览器执行）

- [ ] **新生全流程**：启动 → 填 Key → 测试 → 上传题+Excel → 整体解读（流式）→ 划词 → 角色卡 → 追问 → 卡片 → 编程出图
- [ ] **错误路径**：坏 Key / 断网 / 不支持的文件 / 超大文件 / AI 超时 / LLM 返回坏格式
- [ ] **数据持久**：刷新、关浏览器重开、会话列表继续/删除、导出 MD 可打开
- [ ] **兼容**：Chrome/Edge；375px 与 1440px 视口
- [ ] **流式体验**：三处打字机输出正常；无流式能力时自动整段输出
- [ ] **卡片交互**：决策树动画播放/暂停/重播；K-means 拖点重聚类；收藏刷新后保留

## 4. 发布检查表（§7 M6 出口）

| 项 | 状态 |
|----|------|
| README（教程）+ MIT License + .gitignore | ✅ 已有 |
| CI（lint+test） | ❌ 未配置（个人公益项目可后置） |
| 第三方许可核查（THIRD_PARTY_NOTICES） | ✅ 已有；Pyodide MPL-2.0 / Plotly MIT / ECharts Apache-2.0 已注明 |
| 隐私声明（Key 只存本地 + 加密） | ✅ README 与设置页均已注明 |
| 无障碍抽查 | 🖥 代码已就位，待人工过一遍 |
| 打包产物：`npm run build` 无报错 | ✅ 36 模块，754ms |
| 首发 tag（v0.1.0）+ Release Notes | ❌ 未打 tag |

## 5. 遗留与风险

1. 图片 OCR（F-101）未实现——PRD 标注可延后，README 已注明
2. 「对话中切换模型」（F-504）未实现
3. 卡片导出 Markdown、Docker 沙箱、深色模式为待办
4. 无自动化测试框架（§6.1 高风险模块用例未落地为代码），当前以接口实测 + 评测脚本覆盖
5. 浏览器交互类验收项（🖥）需按 §3 清单人工走查后勾选

---

*本报告随验收迭代更新；🖥 项完成后请将状态改为 ✅。*
